<% if resource.errors.any? %>
  <% resource.errors.full_messages.each do |msg| %>
    <%= javascript_tag "M.toast({
      html: '#{msg}',
      classes: 'red ',
      displayLength: 2000
      });" %>
  <% end %>
<% end %>
